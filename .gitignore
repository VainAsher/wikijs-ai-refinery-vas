# --- Runtime data (regenerated on first run; never commit) -------------------
# The SQLite store, saved settings (Wiki.js/Ollama tokens), exports, and the
# default context packs are all created/refreshed at startup, so the repo ships
# data-free and a fresh clone seeds itself.
/data/
data/

# --- Local environment / secrets --------------------------------------------
.env
.env.*
!.env.example
!.env.docker.example
# Auto-generated symmetric key used to encrypt secrets at rest
.secret_key
**/.secret_key

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# --- Virtual environments ----------------------------------------------------
.venv/
venv/
env/
ENV/

# --- Tests / coverage --------------------------------------------------------
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# --- Deploy state (created on the homelab host by scripts/deploy.sh) ---------
/.deploy/
/logs/
# Default placeholder mount for source Markdown (set REFINERY_IMPORTS_DIR instead)
/imports/

# --- Editor / OS -------------------------------------------------------------
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# --- Claude Code local settings (machine-specific permissions) ---------------
.claude/settings.local.json
